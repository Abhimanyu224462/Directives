import { MyCustom4Directive } from './my-custom4.directive';

describe('MyCustom4Directive', () => {
  it('should create an instance', () => {
    const directive = new MyCustom4Directive();
    expect(directive).toBeTruthy();
  });
});
