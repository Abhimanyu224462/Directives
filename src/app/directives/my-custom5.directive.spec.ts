import { MyCustom5Directive } from './my-custom5.directive';

describe('MyCustom5Directive', () => {
  it('should create an instance', () => {
    const directive = new MyCustom5Directive();
    expect(directive).toBeTruthy();
  });
});
