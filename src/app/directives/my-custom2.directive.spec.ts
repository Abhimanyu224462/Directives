import { MyCustom2Directive } from './my-custom2.directive';

describe('MyCustom2Directive', () => {
  it('should create an instance', () => {
    const directive = new MyCustom2Directive();
    expect(directive).toBeTruthy();
  });
});
