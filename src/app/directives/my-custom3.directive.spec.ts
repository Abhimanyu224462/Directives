import { MyCustom3Directive } from './my-custom3.directive';

describe('MyCustom3Directive', () => {
  it('should create an instance', () => {
    const directive = new MyCustom3Directive();
    expect(directive).toBeTruthy();
  });
});
